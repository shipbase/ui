---
import { cn } from "@/lib/utils";

const { className, ...props } = Astro.props;
---

<a
  class={cn(
    "bg-accent text-accent-foreground hover:bg-surface/80 flex w-full flex-col items-center rounded-xl p-6 transition-colors sm:p-10",
    className
  )}
  {...props}><slot /></a
>
