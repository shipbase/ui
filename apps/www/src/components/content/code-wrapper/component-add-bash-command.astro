---
import BashCommand from "./bash-command.astro"

interface Props {
  name: string
}

const { name } = Astro.props

const src = `npx shadcn@latest add @shipbase/${name}`
---

<BashCommand src={src} lang="bash" className="mt-0" />
