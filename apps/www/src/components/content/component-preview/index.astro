---
import ExampleSource from "@/components/content/example-source.astro"
import { cn } from "@/lib/utils"
import Preview from "./preview"
import Tabs from "./tabs"

export interface Props {
  name: string
  className?: string
}
const { name, className } = Astro.props
---

<div
  class={cn(
    "relative my-4 flex flex-col space-y-2 lg:max-w-[120ch]",
    className
  )}
>
  <Tabs client:load>
    <Fragment slot="preview">
      <div
        class="relative flex size-full h-[450px] w-full items-center justify-center rounded border p-4 md:p-10"
      >
        <Preview client:only="react" name={name} />
      </div>
    </Fragment>
    <Fragment slot="code">
      <ExampleSource name={name} />
    </Fragment>
  </Tabs>
</div>
