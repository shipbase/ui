---
import { loadFileSource } from "@/lib/utils"

import CodeWrapper from "./code-wrapper.astro"

export interface Props {
  path: string
}
const props = Astro.props

const source = await loadFileSource(props.path)

const lang = props.path?.split(".").pop() as any
---

<CodeWrapper lang={lang || "plantext"} src={source} />
